export enum DbStoreKey {
  users = 'users',
  artists = 'artists',
  albums = 'albums',
  tracks = 'tracks',
  favourites = 'favourites',
}

export enum DbMessage {
  wrongPassword = 'WRONG_PASSWORD',
}
